version: '3.8'
services:
  grafana:
    image: grafana/grafana-oss
    container_name: grafana-netdev
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - ./grafana_dashboards:/var/lib/grafana/dashboards
    restart: unless-stopped