version: '3.8'

services:
  prometheus:
    image: prom/prometheus:latest
    container_name: netdev_prometheus
    restart: unless-stopped
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"